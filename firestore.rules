rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All direct client-side access is disabled.
    // All database operations must flow through the server-proxy API.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
